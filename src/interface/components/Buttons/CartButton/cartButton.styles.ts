import { makeStyles } from "@material-ui/core/styles";

const useCartStyles = makeStyles(() => ({
	iconButton: {
		backgroundColor: "rgb(243, 245, 249)",
		marginLeft: 16
	}
}));

export default useCartStyles;
